# Leave VITE_API_BASE_URL empty to use the local Vite /api/v1 proxy.
# Set it to an absolute API origin only when the backend is served elsewhere.
VITE_API_BASE_URL=
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8000
